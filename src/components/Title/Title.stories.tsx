import { PageTitle } from '../../App'
import  {PageTitleSecret} from './Title'

export default {
  component: PageTitleSecret,
}

export const Title = () => {
  return <PageTitle title ={'Some Title'}/>
}